.side
  .side__bar
    .side__bar_user-name
      .top
        .top__items
          .top__items_name
            = current_user.name
          .top__items_icons
            = link_to "#" do
              = icon('fas', 'edit')
            = link_to edit_user_path(current_user) do
              = icon('fas', 'cog')
    .side__bar_group-list
      .title
        techcamp
      .message
        no-message
      .title
        techcamp
      .message
        no-message
      .title
        techcamp
      .message
        no-message